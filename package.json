{
  "name": "velora-secrets",
  "version": "1.0.0",
  "private": true,
  "description": "Velora Secrets — webshop (statische site + Vercel serverless API-routes)",
  "scripts": {
    "dev": "vercel dev",
    "generate:catalog": "node scripts/generate-catalog.js"
  },
  "dependencies": {
    "pg": "^8.11.0"
  }
}
